# 2026-08-27 — Project Bootstrap

完成了project-bootstrap计划的Task 1-6、9(本地部分)。

## 完成内容

- GitHub repo `austinxyz/zijing-cup`(public)建好
- Backend: FastAPI + uv骨架，`/health`端点(schema-qualified Supabase连接 + 共享密钥中间件)，10个测试全过
- Frontend: Next.js骨架，`lib/api.ts`(`getHealth()`) + 占位首页，5个测试全过
- Supabase: 本地CLI stack跑通，`zijing_cup` schema建好并verified
- 本地端到端验证通过：frontend(localhost:3010) → backend(127.0.0.1:8010) → 本地Supabase(zijing_cup schema)，首页正确显示"Backend: ok" / "Database: ok"

## Task 7：部署接线（用户手动完成）

- 远程Supabase `zijing_cup` schema：**没有用`supabase db push`**（会跟共享项目里
  ai-course-management的migration历史冲突），改成Dashboard SQL Editor手动执行
  `create schema if not exists zijing_cup;`。规则记入CLAUDE.md。
- Render: https://zijing-cup-api.onrender.com （`/health` → `{"status":"ok","db":"ok"}`）
- Vercel(前端): https://zijing-cup-frontend.vercel.app/ （显示"Backend: ok" / "Database: ok"）
- 全链路验证通过：Vercel → Render → 远程Supabase(`zijing_cup` schema)

**Bootstrap全部完成，可以开始下一个change：`roster-import`。**

## 遇到的坑(已记入CLAUDE.md)

- 本机Application Control策略拦截`uv run uvicorn`直接执行，改用`uv run python -m uvicorn`绕过
- 本机8000端口常被占用，本地验证改用8010
- 共享Supabase项目禁止用CLI的`db push`/`migration repair`，migration手动去Dashboard SQL Editor跑

## 待最终review的技术债(非阻塞，已在最终review中部分修复)

- `backend/tests/test_db.py`的search_path测试靠读取SQLAlchemy `pool._creator`闭包内部实现验证，比较脆弱(但不是no-op)，留到`roster-import`加DB fixture时换成真连接测试
- 无CI(final review建议，非必须)
- `render.yaml`没锁Python版本(final review Minor)
- `frontend/next-env.d.ts`没加进`.gitignore`(final review Minor)
