# 2026-08-27 — Project Bootstrap

完成了project-bootstrap计划的Task 1-6、9(本地部分)。

## 完成内容

- GitHub repo `austinxyz/zijing-cup`(public)建好
- Backend: FastAPI + uv骨架，`/health`端点(schema-qualified Supabase连接 + 共享密钥中间件)，10个测试全过
- Frontend: Next.js骨架，`lib/api.ts`(`getHealth()`) + 占位首页，5个测试全过
- Supabase: 本地CLI stack跑通，`zijing_cup` schema建好并verified
- 本地端到端验证通过：frontend(localhost:3010) → backend(127.0.0.1:8010) → 本地Supabase(zijing_cup schema)，首页正确显示"Backend: ok" / "Database: ok"

## 待办

- **Task 7(部署接线)留给用户手动做**：`supabase link`需要输入DB密码、Render/Vercel控制台需要粘贴环境变量——这些是凭证类操作，agent不能代劳。步骤见`docs/superpowers/plans/2026-08-27-project-bootstrap.md`的Task 7。
- Task 7做完后回来补Task 9 Step 7：把部署好的Render/Vercel URL记进本文档或requirements.md。

## 遇到的坑(已记入CLAUDE.md)

- 本机Application Control策略拦截`uv run uvicorn`直接执行，改用`uv run python -m uvicorn`绕过
- 本机8000端口常被占用，本地验证改用8010

## 待最终review的技术债(非阻塞)

- `backend/tests/test_db.py`的search_path测试靠读取SQLAlchemy `pool._creator`闭包内部实现验证，比较脆弱(但不是no-op)
- `frontend/app/page.tsx`的catch块吞掉了真实错误详情(plan brief自带的写法，不是implementer引入的)
