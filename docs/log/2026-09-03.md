# 2026-09-03

## lineup-single-pin（单座位 pin：钉一人到线、引擎配搭档）

opsx 四阶段走完并归档（`2026-09-03-lineup-single-pin`）。commit `6be71d1`(req) →
`64a3a6a`(propose) → `c4a9b64`(后端) → `cbd60a3`(前端) → `e5e0292`(验证) push master，
自动部署 Vercel + Render。无 migration。

### 起因

用户报 2026 银组 ZJU-USC「每条线只指定一个人、系统算不出合适阵容，实际上有」。系统调试
（systematic-debugging）查明**不是计算 bug，是功能缺口**：当前「锁定」是整对（一条线两个
座位），半填的线被 `constraintsFromQuery` 无声丢弃（`if (first && second)`），后端
`_parse_locks` 也要求两个 key。所以「钉一人、引擎配搭档」这个意图根本无法表达，引擎忽略
半填、给出没照钉法的阵容。→ 开这个 change 补齐。

### 做了什么

- 后端 `search_lineups` 收 `pins`（line→被钉者）：被钉线 `options[L]` = 含被钉者的合法对
  （把被钉者临时并回池子跑 `legal_pairs` 再筛），被钉者从其它线的 `available` 剔除、搭档
  现选、递归 used 集兜底；strongest/scarcest 排序不变。硬锁整对不变，pin/硬锁/排除可组合，
  女将可钉男双。
- `diagnose_line` 加 `pinned`：pin 那条线无解时只在含被钉者的对里跑四关分类、message 点名
  （`你把 X 钉在 L…`），不报无关的整池「本可行」原因。
- 冲突输入（同一人钉两线 / pin∩排除 / pin∩硬锁成员 / 一线两座同人）在 `search_team_lineups`
  抛 422。端点 `pin=LINE:key`（不重载 `lock=`）。
- 前端 `constraintsFromQuery` 分三态（一座位=pin / 两座位=硬锁 / 同人=无约束）；`lib/api`
  编码 pin；控件「已钉」badge + 「搭档交给引擎」小字（硬锁「锁整对」）；pin 无解走既有
  `NoSolution`（后端 message 已点名）。

### 应用期踩的坑

1. **`Candidate.name` 是 `last\tfirst` 制表符拼的**——pin 诊断 message 直接用 `pinned.name`
   在页面上显示成 `Hu\tMitch`。这是第二次栽（上次 infeasibility-detail 归因）。加了
   `_display_name` helper 格式化。已沉进 CLAUDE.md pitfalls。
2. **`reseed-local.sh` 用 `uv run python`，本机已被 Application Control 拦**（`os error 4551`）。
   改成手动用 `backend/.venv-std/Scripts/python.exe -m app.seeds.load_rules`（rosters/team_names/
   players.migrate 同）按序补种。已沉进 CLAUDE.md pitfalls。
3. **女将会救男双线的 pin 无解场景**——构造 pin-over-cap 测试时，男双线允许女性填座，最低
   UTR 的女将把 pin 的和拉回 cap 内。改用 MD（混双，搭档限女）才逼出全 over-cap。

### 验证

group 1 eval 99、group 2 eval 100，均 attempt 1 PASS 无 BLOCK。后端 pytest 467、前端
vitest 391、tsc 干净、无 console.log、无泄漏。真数据实测（本地 2025 SJSU）：pin 一人控件
「已钉」badge(4.89)+hint；Hu Mitch 钉 MD → 无解面板点名 over_gap+over_cap（16.15）；桌面 +
375 无横向溢出。

## lineup-saved-lineups（保存具体阵容 + 重判页 + 就地编辑器）

opsx 四阶段走完并归档（`2026-09-03-lineup-saved-lineups`）。5 组 EVAL 全 PASS（G1/G2 后端、
G3/G4 前端均 99 分）。commit `880ae75`(req/propose) → CRUD 路由 → G3 保存入口+已存阵容页 →
G4 编辑器 → G5 验证 → 收尾追加「UTR/性别/线总和/buffer」(`a0ee305`)，push master。
无新 migration（line_totals 纯计算，不动 schema）；saved_lineups 表的远程迁移用户已在
Dashboard 手动执行。

### 做了什么

- **后端**：`saved_lineups` 表（assignment + utr_snapshot 两列 JSONB）；`app/lineups/saved.py`
  存/列/删 + `revalidate_saved`（按当前 UTR 跑 `check_lineup` 分四态：valid/utr_moved/illegal/
  player_gone，快照只用来点名 diff，绝不当合法性依据）+ `assignment_violations`（校验端点复用
  check_lineup）。CRUD + `/validate` 路由，写方法自动受 admin 中间件保护。收尾让 revalidate
  复用 `pair_total`+caps 顺带算 `line_totals`/`buffer_spent` 出到响应。
- **前端**：候选行「保存此阵容」（admin，服务端拍快照）；`lineup/[code]/saved/` 页四态卡片 +
  逐人 UTR/性别 + 线总和/超 cap + buffer + 一键载入（五线锁进 URL）+ 删除；`LineupEditor`
  互换/替换 + 防抖 300ms 实时 `POST validate` + 存回（PUT 重拍快照）。

### 应用期踩的坑（已沉 CLAUDE.md）

1. **render-prop 函数从 Server Component 传给 Client Component 运行时崩**（vitest 测不出，
   整页掉 error boundary）。改传 canEdit + 序列化 server action。
2. **未知后端 status `?? BADGE.valid` 是 fail-open**——糊成「仍合法」。改「未知状态」不可载入
   + 类型收成 literal union。触摸目标 <44px 一并修（min-h-11）。以上两条是 group-3 嵌套
   code-review **在评审 PASS 之后**迟到报上来的 2 个 HIGH，照「PASS 不等于清零」处理。
3. **登录口令查 `ADMIN_PASSWORD_HASH`（scrypt）不是 `ADMIN_SECRET`**；in-app 浏览器验 admin
   要临时生成 hash 写 .env.local 并重启 dev server。
4. 两个误提交的构建产物（root `node_modules/.vite`、`frontend/test-output.txt`）untrack + ignore。
