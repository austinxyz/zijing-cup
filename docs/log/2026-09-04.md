# 2026-09-04

## lineup-page-defaults（默认视图重排 + 统一三行块 + 就地编辑）

opsx 四阶段走完并归档（`2026-09-04-lineup-page-defaults`）。propose→apply 四组
（默认视图 / 三行块 / 载入保存 / 就地解锁）+ 验证，四组 EVAL 全 PASS（87/97/96/93）。
commit `be0a08c`(propose) → `aa4ac25`(g1) → `234ec4e`+`5e993a0`(g2) → `509030e`(g3) →
`3381940`(g4) → `f9e0000`(refactor) → `c21bea2`(g5) push master，随后四处交付后修复
`383d827` `e1e7b83` `72c1cf4` `4a7872e`。纯前端 + 一处后端响应加字段，无 migration。

### 起因

用户提的一批排阵页小改：① 有已存阵容时默认直接看它、要算再搜；② 载入阵型只预填控件、
点搜索才算、也能存；③ 候选完整显示两名搭档姓名+UTR+性别；④ 加「编辑模式」输 admin
口令改阵容。设计评审时把「候选/已存阵容统一每条线三行块、五块横排」定了下来。

### 做了什么（四组）

- **默认视图**：`page.tsx` 读 `searchParams.go` 门控——无 go 不整解，只读已存阵容+roster
  （走 `getTeamRoster`，无 solve）渲染右栏上半（`CollapsibleSaved` 可折叠）+ 下半空态；
  有 go 走现状（候选 + 无约束基线并发）。搜索按钮带隐藏 `go=1`。
- **三行块**：抽共用 `LineBlock`（线名+和+buffer 占用；两名队员各一行 姓名+♂/♀+UTR），
  候选 `CandidateCards` 与 `SavedLineups` 都用它、五块横排；`--color-male/--color-female`
  token 实测 ≥4.5:1；旧对比表 `CandidateTable`/`CandidateRow` 退役。
- **载入保存**：载入 preset 复用 `buildLoadHref`（不带 go）→ 草稿预填控件；存/另存复用现有
  Presets 输入（同名覆盖、新名另存），不要求先搜候选。
- **就地解锁**：`EditModeToggle` 页头开关→输口令→新 `unlockAdmin`（与 `login` 共用
  `authenticate` 核心，只是返 `{ok}` 不 redirect）→ `router.refresh` 就地生效。

### 交付后修的四处（都线上验证过）

1. **载入不刷控件**（`383d827`）：软导航 `router.push` 复用非受控 `<select>` DOM，
   `defaultValue` 只挂载时生效 → 地址栏变了控件还空，只有整页刷新才回填。修：`LineupControls`
   按约束串 `key` remount。
2. **候选没滚动条**（`e1e7b83`）：`LineupResults` 根残留 `overflow-hidden`（旧 `CandidateTable`
   自带滚动时的写法），换成无内层滚动的 `CandidateCards` 后静默裁剪。修：剥掉内层
   `flex-1/min-h-0/overflow`，让外层单一 `overflow-y-auto` 列滚。
3. **已存阵容缺编辑**（`72c1cf4`）：右栏 SavedLineups 只传了 `deleteAction`，`canUseEditor`
   false → 编辑按钮不出现。修：补 `validateAssignment`+`saveBackLineup` 绑定；删掉「载入」
   （把十人当锁定塞回控件无用），改为就地「编辑」。
4. **已存阵容对齐候选**（`4a7872e`）：卡头加队伍总和（后端 `SavedLineupOut.total`，Decimal
   求和，`player_gone` 为 null）、姓名用「姓 名」。

### 教训（已进 CLAUDE.md Pitfalls）

- 软导航不给非受控输入重套 `defaultValue`——靠软导航改 URL 回填的控件要按参数 `key` remount；
  vitest 无 RSC router 测不出。
- **移除**自带滚动的子组件时，回头查父层遗留的 `overflow-hidden` 会不会静默裁剪（`h-screen
  overflow-hidden` 壳那条规则的对偶）。

### 环境噪声

本机 local supabase（54322）本次多次无响应（连 `count(*)` 都超时），补种/本地视觉核对
做不了，视觉验证改在 Vercel 生产上做。另清了几个上个 session `&` 起 uvicorn/pytest 遗留、
占着 DB 连接的僵尸 python 进程。

## team-roster-editing（队伍页就地编辑 + 外援上场限制进引擎）

opsx 四阶段走完并归档（`2026-09-04-team-roster-editing`）。6 组，每组 EVAL 通过
（1:94 → 2:98 → 3:99(第2次) → 4:96(第2次) → 5:100），前端 457 / 后端 513 测试、tsc 干净。
commit `884cc5f`(propose) → `2c44efe`/`9cd1d13`(g1) → `4fad62d`/`c4a6226`(g2) →
`3cc8737`/`962a163`(g3) → `51ba19a`/`702c090`/`4d41650`(g4) → `d1b6186`(g5) →
`8169376`(g6) push master。**含一个 migration**（`teams.school_count` +
`division_borrowed_limits`）——本地直接打 127.0.0.1，远程负责人在 Dashboard 手工执行后才归档。

### 起因

用户要队伍页可编辑：① 同阵容页的就地口令解锁；② 批量改双打 UTR；③ 标外援/外卡、填代表学校
（后改为：外援/外卡不填学校、本校队员填）；④ 外援按每场上场上限进搜索、候选/已存阵容配色。
关键约束（用户拍板）：外援规则**和赛事绑定**（2026 金/银是这套，明年可能变）→ 数据化按
division 存；学校数是队伍属性、人工填。

### 做了什么（6 组）

- **g1 schema/seed**：`teams.school_count`（可空，null≠0）+ `division_borrowed_limits` 表；
  `load_rules` 解析 toml 的 `[[borrowed_limits]]`、写/比对/漂移检测都带上；2026 金+银 seed。
- **g2 后端写**：`update_membership`（PATCH memberships，borrowed/wildcard 清 school）+
  `set_team_school_count`（PATCH team，ge=1）；批量双打 UTR 复用既有 `PUT /players/current-utr`
  + `_mirror_participation`（赛季锁护栏已在）。roster 响应加 team.id/school_count/borrowed_limits
  + 逐人 is_wildcard/representing_school。
- **g3 引擎**：`Candidate.borrowed`（只 True 计）；`search_lineups(borrowed_cap)` 叶子丢弃超限
  阵容、全空则置 `borrowed_over_limit`；`school_count` 未设→不校验；按 division 查 on_court_cap。
- **g4 队伍页 UI**：`TeamEditPanel`——EditModeToggle 解锁、批量双打输入+一个保存、外援/外卡勾选、
  条件学校、school_count + 上限提示 + 超名单警告放行；`saveTeamEdits` 合批三种写。
- **g5 配色**：LineBlock seat `borrowed` → 外 角标 + 底色；`--color-borrowed`(#9a4410) 进对比度
  测试；候选/已存阵容传后端 borrowed；`borrowed_over_limit` 面板。

### 评审拦下的两处（都记进 CLAUDE.md Pitfalls）

- **g3**：评审第一次要 `is_borrowed_player is not False`（None 当外援、fail-closed）——**拒了**，
  那会让没标过人的队变成十个外援永远无解。改用 `is True`（fail-open，只算确认的），第二次 PASS。
- **g4**：清空双打输入发 `""` → 后端 Decimal 解析 422、批量全有全无 → 整批没写。改成 `""→null`；
  并给 save() 加 try/catch（失败保留改动 + 报错，成功才 reset）。

### 环境坑

后端 push 依赖远程 migration（Dashboard 手工），未跑前线上会 500——已记进 pitfalls。本机
Docker Desktop 中途没开导致本地 supabase 无响应，起来后继续；pytest 每轮清库，视觉核对前补种。

### 归档后跟进修复（team-roster-editing 上线后）

同 change 上线后用户报的几处，都是直接提交到 master（无独立 opsx change）：
- `c1543a2` **存为阵型 丢单-pin**：preset 只存 locks+excluded，`savePreset` 绑定把 `pins`
  丢了——只 pin 一人（没锁两个）的草稿存成空 preset。全链路补 pins（类型/绑定/buildLoadHref
  发 `pin=LINE:key`/presetSize/staleLockRefs）。JSONB，无 migration。
- `7319223` **存的是载入时的旧条件**：`存为阵型` 用 URL 绑定的 constraints，而控件是 GET 表单、
  改动点搜索才进 URL——载入后改条件再存丢了改动。改成保存时读**实时表单**（`constraintsFromForm`），
  按钮只按名字判禁用，空表单给提示。
- `3058ae3` **队伍页只读也显示 外援/外卡/学校** + 页头「编辑模式 ⇄ 查看模式」切换（放队伍名行最右，
  `TeamEditContext` 共享状态）。

### player-win-loss（opsx 四阶段，已归档 2026-09-04-player-win-loss）

队伍页加胜率信息，走 CSV 导入。`players.wins/losses`（可空 int，生涯值、null≠0）+ migration；
导入多读组委会导出表末尾 `胜`(cell 9)/`负`(cell 10)，`总场次`/`胜率` 忽略；进差异屏计数、
随全有或全无落库；导出保持 8 列故自家导出往返对战绩 0 改动。花名册响应带 wins/losses；只读
花名册加「胜率」列（`胜-负` + %，缺失 `—`，真 0-0 无百分比、不除零），桌面 + 手机共用
`lib/winLoss.ts`。5 组 TDD，评审全 PASS（group 3 一次 BLOCK 是把 group-4 的前端类型误算成
group-3 缺口，读 findings 判为误报后前置补上再过）。

坑：真跑页面见 `undefined-undefined`/`NaN%`——后端进程陈旧、响应缺字段，前端 `=== null` 漏过
undefined；改 `== null` 兜住并杀陈旧 uvicorn 重起。两条进 pitfalls（松判空、共享 side-effect
的护栏要盖全路径）。

### player-win-loss 归档后跟进（直接提交 master）

- `0bedb03` **fix: CSV 导入 projected/unrated 双打不得 mirror 进参赛 UTR**。批量/行内路径早有
  `_NON_MIRRORING_STATUSES` 门，sheet `apply_sheet` 漏了——导入一个 projected 双打无声覆盖冻结
  参赛值。补上同一道门。
- `be1e86a` **feat: 编辑模式可直接改参赛 UTR**。显式值直写 season UTR（admin_ruling/待定，走既有
  `PUT /players/{id}/season-utrs/{year}`），排在当前 UTR 批量后压过 mirror；未动沿用 mirror；
  锁季只读（端点 409）；空=不改。前端-only，复用已测端点。

### saved-lineup-order（opsx 四阶段，已归档 2026-09-04-saved-lineup-order）

已存阵容加顺序（拖拽 + 手机 ↑/↓）+ 克隆。`saved_lineups.sort_order`（int not null
default 0）+ migration 按 name 回填保序；list 按 (sort_order,id)；save max+1；重排端点
（整份有序 id，幂等，坏列表 422 整体拒）；克隆端点（逐字节复制 + 副本N 去重 + clamp 60
+ 50 上限）；`SavedLineupOut` 带 sort_order。前端拖拽 + ↑/↓ + 克隆（乐观 + 回滚 + pending
禁用）。5 组 TDD 全 PASS；group 3 一次 BLOCK 是克隆名超 60 字撞 DB check 会 500（真 HIGH，
已 clamp + 近上限测试）。远程 migration 手工执行后 push。

### 归档后直接跟进（提交 master，无独立 opsx）

- `be76dfa` **阵容改名**：`rename_saved_lineup` + `PATCH …/{id}`（重名 409、空/超60 422、
  自名 OK、别队 404，声明在 `…/order` 后免路由撞）+ 内联改名 UI。
- `9db14c2` **排阵页编辑/查看模式**：仿队伍页 `LineupEditProvider`/`HeaderControl`，编辑态才显
  admin 写控件；context 默认 editing:true 使独立 `saved/` 页不变。
- `dd8e27e` **改名 UI 不刷新修复**：`items` 同步键 id→`id:name:sort_order`（改名已落库但显示旧名）。
- `dd60c8f` **队伍花名册去掉「当前单打」列**（只留当前双打；单打该页用不上）。
- 更早本日：`7c29dc4`/`dd6e01d` 阵容高胜率 ▲ 标记（候选/已存/下拉/chip + hover 胜率）。
